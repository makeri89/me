---
export interface Props {
	title: string;
	body: string;
}

const { title, body } = Astro.props;
---

<div class="border-2 border-rose-600 rounded-lg w-full max-w-xs sm:max-w-md p-4 text-center shadow-card">
		<h2 class="text-2xl mb-3">
			{title}
		</h2>
		<p>
			{body}
		</p>
</div>